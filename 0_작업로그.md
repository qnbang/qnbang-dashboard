# 작업 로그 — 큐앤뱅 사내웹 대시보드

> 프로젝트의 줄거리. 작업할 때마다 위에서부터 최신순으로 한 줄씩 남깁니다.
> 정확한 변경 내역(누가·언제·무엇)은 깃 히스토리에, 흐름·결정은 여기에.

---

## 2026-07-07

**정산 탭 = 회사 돈 전체 뷰로 확장 — 인건비 원장·10일 정산·미지급금(역미수)**

- **인건비 탭 신설**(지출장부 시트): 월·구분(직원/프리랜서/기타)·이름·세전·공제·실지급·지급상태·지급일·비고. 기존 드라이브 엑셀(【큐앤뱅】2026 인건비) 2~5월 16행 이관, 행별·월별 합계 검산 통과(실지급 총합 12,223,138원). 엑셀은 과거 기록으로 보존.
- **10일 정산 카드**: 이번달 인건비 명단(지난달 명단 복사 버튼·개별 추가, 프리랜서 3.3% 공제 자동계산) + 원천세(전월 프리랜서 공제 합, 2026-06분부터 추적) + 부가세 적립 권장액(전월 입금분, 지출 아님 표시만) + 고정비 납부일순(기록됨 ✓).
- **지급확인 버튼**: 통장에서 실제 이체된 뒤 눌러야 그 달 지출탭에 기록 — 통장단일원장 유지. 비고 멱등키로 두 번 눌러도 중복 없음(실검증: pay 2회→지출 1행, 복사 2회→409).
- **미지급금(줘야 하는데 안 준 돈)** 스코어카드 + 클릭 목록(지급확인·편집), 미수금 카드와 대칭. 손익 카드에 예상손익(실제+받을 돈−줄 돈) 병기.
- 고정비 탭 4대보험 활성화(납부일 10일, 640,000원 — 고지서와 다르면 탭에서 수정). 공과금은 변동이라 비활성 유지(나갈 때마다 지출 기록).
- ⚠️ 로컬 dev/build는 터보팩 한글경로 버그로 죽음 → `npm run dev -- --webpack`으로 우회(배포는 Vercel 원격빌드라 무관).
- **지급 시점 교정(사장님 지적)**: 귀속월 M분은 M+1월 10일 지급 — 정산 카드를 "이번달 귀속분"에서 **지급 안 된 인건비 전부**(귀속월 뱃지)로 교체, 복사·추가 기본 귀속월=지난달. **원천세 재교정(사장님 2차 지적)**: 납부 규칙이 아니라 실제로 밀린 것 — 지급완료 프리랜서 3.3% 중 미납분 월별 전부 표시(2~5월 160,050원), 대기 건은 지급확인 후 등장. 6월분 입력: 김지영(대기) + 미지급 프리랜서 4건(송차은 2건·김준엽·송광범, 세전 기준 3.3% 공제, 대기 합 5,654,597원). 미지급금 5,733,797원(원천세 포함) 운영 확인.

## 2026-07-06

**"할 일 추가하면 기존 할 일 싹 사라짐" 신고 — 완수·삭제 전체 덮어쓰기 구멍 수리**

- **진단(리비전 대조)**: 구글시트 리비전을 시각별로 내려받아 사고 전후를 비교 — **이날 실데이터 유실은 없었음**. 사라져 보인 건 ①전날 진단 세션이 소프트삭제(칸 비우기)로 남긴 **유령 빈 행 2개**가 단건 할 일 칸에 빈 카드로 떠 있다가 정리된 것 ②새로 넣은 할 일이 캐시 지연으로 늦게 뜬 것이 겹친 착시.
- **다만 구조 구멍은 진짜였음**: 완수(✓)·삭제가 "과업 탭 전체를 읽고 → 비우고 → 되쓰기"라, 두 요청이 겹치면 지운 행이 부활하고(아카이브 같은 id 중복 4쌍 물증), 되쓰는 몇 초 사이에 추가된 행은 증발하는 경쟁 조건.
- **수리**: ①완수·삭제를 **SA Sheets API 단일행 삭제**(deleteDimension)로 교체 — 아카이브 append 성공 후에만 원본 삭제, 삭제 직전 id 재조회로 행 밀림 방지, 이미 없으면 멱등 ok(더블클릭 중복 방지) ②리더가 id만 남은 유령 행을 스킵(빈 카드 재발 방지). 로컬에서 추가→완수→재완수(멱등)→삭제 풀사이클 실검증 완료(시트 36행 무결 확인).
- 실패도감 #72 등록. 남은 것: 시트 아카이브의 유령·중복 잔재 6행 청소(사용자 승인 대기), 영업 탭(sales/win)도 같은 덮어쓰기 패턴이라 추후 같은 방식 교체 권장.

## 2026-07-03

**보드 카드 실종 버그 수정 (자체사업 보류·예정 이동 시 사라짐)**

- **증상**: 대기 칸의 카드(반보)를 보류로 드래그하니 보드 어디에도 안 보임. 데이터(시트)는 정상 — 화면 필터 문제.
- **원인**: 전체 보기에서 자체사업 분류 카드는 "활성 공위치(받은일·작업·처리·대기)"일 때만 보드에 올리는 필터가 있어, 보류·시작전으로 옮기는 순간 메인 보드에서 제외됐음(자체사업 섹션·분류 칩 뷰에만 남음).
- **수정**: 보드 필터를 "도구·리서치만 제외"로 단순화 — 자체사업도 모든 공위치(보류·예정 포함)가 해당 칸에 보임. 완수는 원래 서버에서 아카이브로 빠지므로 영향 없음.
- **같이 잡은 논리 오류**: 가동률(진행 중 프로젝트) 계수에 보류(hold)가 포함돼 멈춘 프로젝트가 '가동'으로 잡히던 것 제외. 미사용 상수(PORTFOLIO_CATS) 정리.
- 배포 완료(vercel CLI, office-view 브랜치).

**노션식 공유 토글 완성 (보드개선 계획 1번)**

- **문서 모달에 🔗 공유 토글 추가**: 프로젝트 문서를 열고 버튼 한 번 → 외부 공개 링크 생김, 다시 누르면 죽음. 노션 "웹에 게시"와 같은 UX. 공개 중이면 링크+복사 버튼 노출.
- **공유 상태 저장 = 상태 전용 repo**: `qnbang-dashboard-data` repo의 `share-registry.json`에 slug 단위 기록(`lib/share.ts`). 기존 `GITHUB_TOKEN` 쓰기 권한 재사용, sha 충돌 방지, 16자리 무작위 slug. 토글=이 파일 커밋 → **런타임 no-store 읽기라 배포 없이 즉시 반영.** (계획서는 구글시트였으나 현 시트가 읽기전용 Apps Script라 셋업 0인 GitHub로 변경.)
  - **⚠️ 왜 코드 repo가 아니라 별도 repo?** 처음엔 코드 repo(`qnbang-dashboard`)에 뒀는데, 그러면 사람(로컬 커밋)과 서버(런타임 커밋)가 같은 repo에 써서 로컬·origin이 갈라진다(diverge). 사람이 안 만지는 상태 전용 repo로 분리하니 코드 repo는 한 줄로 흐른다. 런타임 데이터는 앞으로도 여기에.
- **동적 공유 페이지** `app/share/[slug]/page.tsx`: 레지스트리 확인→공개면 GitHub서 원본 MD fetch→렌더, 비공개면 404. 변환기(`.tools/md-읽기페이지/변환.mjs`) 렌더 로직을 `lib/md.ts`로 포팅(의존성 0). **QN! 로고**를 상단·하단에 박아 정적 공유물(씨투아 협업본)과 같은 톤 — 로고는 공개 경로인 `public/share/qn-logo.png`에 둠(외부 열람자도 보이게).
- **토글 API** `app/api/share/route.ts`: 상태 조회(GET)·토글(POST), 로그인 보호. 공개 페이지(`/share/*`)만 인증 제외(기존 middleware 그대로). **공유 링크는 항상 `dashboard.qnbang.com`으로 고정**(vercel.app로 접속해 토글해도 우리 도메인으로 통일). 배포 스크립트 완료 메시지 주소도 우리 도메인으로 수정.
- 배포·라이브 풀사이클 검증 완료: 로그인→켜기(URL=dashboard.qnbang.com)→라이브 200·로고 표시·본문 렌더→끄기→404. 본체 보호 유지, 기존 정적 공유물 200 공존, 로고 파일 200.
**대시보드 문서 편집 완성 (보드개선 계획 2번, 2026-06-16)**

- **문서 모달에 ✏️ 편집 추가**: 문서를 열고 편집 누르면 원문이 textarea로 → 고치고 저장하면 GitHub에 커밋(폰·외부에서도 편집 가능). 취소 가능.
- **sha 기반 충돌 방지(핵심)**: 불러올 때 받은 sha를 저장 때 되돌려줌 → 그 사이 origin이 바뀌었으면 GitHub가 409로 거절 → 화면에 "다른 곳에서 먼저 수정됨, 다시 열어 확인" 경고만 띄우고 **절대 덮어쓰지 않음**. 코드: `lib/github.ts` `getDocFull`·`saveDoc`, `app/api/git-projects/[repo]/doc` GET(sha 포함)+PUT.
- **원본 하나 원칙(역할분담)**: 로컬 옵시디언=정밀편집, 대시보드=보고+공유+급할 때 빠른 수정. 대시보드 편집은 origin에 바로 커밋되므로, 로컬에서 이어 작업하려면 **먼저 `git pull`**(저장 후 화면에도 안내문 표시). sha 충돌검사 + git의 non-fast-forward 거절이 양방향 데이터 유실을 막음.
- 라이브 풀사이클 검증: 로그인→불러오기(sha)→저장 성공→**옛 sha로 재저장 시 409 거절(덮어쓰기 방지 확인)**→원문 복원. (상태 전용 repo README로 테스트, 프로젝트 repo·로컬 안 건드림.)

- **남은 일**: 자동만료·비번 옵션은 추후. 인라인 필드편집(제목·메모만)·WYSIWYG는 필요 시.

**갈라짐 해결 + 백업 표준화 + 3번(자동배포) 검증 (2026-06-16)**

- **코드 repo 갈라짐 실제 발생→해소**: 공유토글을 데이터 repo로 옮기기 전(첫 배포 직후) 라이브에서 공유를 켰다 꺼서, 그 런타임 커밋(share-registry.json)이 코드 repo origin에 4개 박혔고, 로컬 배포 커밋 4개와 갈라져 있었음(=사장님이 우려한 그 현상). 겹치는 파일 없어 안전 병합 → 이제 죽은 파일이 된 코드 repo의 `share-registry.json` 제거(상태는 `qnbang-dashboard-data`로 이전됨) → push로 **0/0 통일·백업**.
- **배포 스크립트에 자동 push 추가**: `대시보드 배포.command`가 커밋 후 `git push origin HEAD`로 **GitHub 백업**까지(CLAUDE.md "push=백업" 표준 충족). 런타임 상태가 별도 repo로 빠져 코드 repo는 코드만 → 다시 갈라질 일 없음. push 실패 시 경고만 띄우고 배포는 계속.
- **공유 페이지 링크 보안**: 외부 공개 페이지라 `lib/md.ts`에서 링크 주소를 http(s)·mailto·앵커·상대경로만 허용(`javascript:` 등 위험 스킴 차단).
- **3번(깃 자동배포) 검증 = 보류 확정**: Vercel은 `chalrie92` 개인 **Hobby(무료)** 범위인데 코드 repo는 **qnbang 조직 private** → 네이티브 자동배포는 사실상 Pro($20/월) 영역(계획서 진단 정확). 무료 대안(GitHub Actions+vercel deploy)은 VERCEL_TOKEN 시크릿·CI 빌드 셋업이 필요. **백업이라는 유일한 실익은 위 자동 push로 무료 해결** → 자동배포는 불필요, 수동 `대시보드 배포.command` 유지가 맞음.

## 2026-06-13

**프로젝트 탭 = GitHub 작업로그 허브 + 드라이브 산출물 미리보기 완성**

- **프로젝트 탭을 GitHub 기준으로 전환**: `qnbang-project` 토픽 붙은 private repo를 자동 수집(`lib/github.ts`, 검색 API 아닌 목록 API로 즉시 반영). repo 루트 `0_작업로그.md`(H1=프로젝트명) + 최근 커밋을 런타임 fetch → 카드+상세. 새 repo에 토픽만 달면 재배포 없이 등장.
- **드라이브 파일 저장 방식 결정**: 서비스계정은 저장공간 0 → 파일 업로드 불가. 공유 드라이브는 요금제에 없음. R2 등 검토했으나 디자인 대용량엔 기존 5TB 구글이 비용 압도적 → **구글 드라이브 + Apps Script(사장님 계정 권한 실행) 출입문** 방식 확정.
- `문서/드라이브-AppsScript.gs`: 폴더생성·업로드·목록 웹앱. 사장님이 배포(웹앱 URL). `lib/drive.ts`는 서비스계정 대신 이 웹앱 호출로 교체.
- **산출물 미리보기**: 프로젝트 repo의 `프로젝트.json {driveFolderId}` → 상세에서 드라이브 폴더 내용을 썸네일 그리드로(폴더 드릴인). 이미지는 `drive.google.com/thumbnail?id=` 썸네일.
- 온보딩 1차: **촌캉스 빈집사업**(repo), **소리쉼**(repo + 드라이브 `2.소리쉼` 폴더 + 랜딩이미지 5장 업로드, 미리보기 확인). 둘 다 토픽 부착·라이브.
- 환경변수 추가: `GITHUB_TOKEN`·`GITHUB_OWNER`·`DRIVE_SCRIPT_URL`·`DRIVE_SCRIPT_KEY`. (기존 `GOOGLE_SERVICE_ACCOUNT_JSON`·`DRIVE_*_FOLDER_ID`는 드라이브 SA방식 폐기로 미사용.)
- **남은 일**: ①나머지 11개 프로젝트 repo 온보딩(소리쉼처럼 이미지는 .gitignore) ②작업 끝나면 자동 커밋·푸시 훅(목표 1번 '자동' 완성) ③드라이브 산출물 자동 업로드 연결 ④팀원 공유(드라이브 루트 `큐앤뱅 작업파일` 팀에 공유→썸네일 노출) ⑤구 SA폴더(`99.클로드 대시보드 테스트` 안 2026대행/자체/18.소리쉼) 정리.

## 2026-06-12

**프로젝트 탭 + 구글 드라이브 연동 구현 (1차, 이후 GitHub기준으로 재설계)**

- `lib/drive.ts` 신설: 서비스 계정 기반 Drive API 유틸 (폴더 목록·파일 목록·폴더 생성)
- `/api/projects` GET/POST: 드라이브 폴더를 프로젝트 목록으로 읽기 + 새 프로젝트 생성 (소재/결과물/공유본 자동 생성, 번호 자동 채번)
- `/api/projects/[id]/files` GET: 프로젝트 폴더 파일 목록
- 대시보드 "프로젝트" 탭 추가: 대행·자체 구분, 카드 클릭 시 파일 인라인 표시, 새 프로젝트 모달
- **배포 전 필요한 환경변수 설정** (아래 참고)

### 환경변수 설정 방법 (Vercel)

| 변수명 | 값 |
|---|---|
| `GOOGLE_SERVICE_ACCOUNT_JSON` | `google-key.json` 파일 내용 전체를 한 줄 문자열로 |
| `DRIVE_2026_FOLDER_ID` | 드라이브 `1.2026 대행` 폴더 URL의 마지막 ID |
| `DRIVE_SELF_FOLDER_ID` | 드라이브 `0.자체 프로젝트` 폴더 URL의 마지막 ID |

**서비스 계정에 드라이브 폴더 공유 필요**: `1.2026 대행`, `0.자체 프로젝트` 폴더를 서비스 계정 이메일에 "편집자" 권한으로 공유.

## 2026-06-11

**업무툴에 SEO 상품 작명기 추가**

- 업무툴 탭 도구 목록(`WORK_TOOLS`)에 SEO 상품 작명기(https://qnbang-seo-namer.vercel.app) 카드 추가
- 도구 원본은 `큐앤뱅-서비스개발/SEO상품명작명` 프로젝트 (2026-06-11 배포 완료된 것을 연결만 함)

## 2026-06-10

**탈노션 → 대시보드에 프로젝트·문서 허브 통합 (방향 확정, 빌드 전)**

- 노션 이탈 이유: ①MD로 작업·공유 ②우리도 MD 편히 보기 ③비용 0 ④버전관리·로그가 작업과 함께 남음(노션은 수동 입력)
- 실시간 동시 편집은 불필요 → 각자 쓰고 댓글·공유로 합의
- 결정 구조:
  - 문서는 프로젝트 폴더의 마크다운 원본, 옵시디언으로 보기·편집
  - **프로젝트별 private GitHub repo** ([[깃-배포-표준]]에 맞춤 — 워크스페이스 통째 한 repo 아님)
  - 대시보드에 **"프로젝트" 메뉴** 추가, 각 repo의 `공유: true` 문서를 **GitHub API 런타임 fetch**로 모아 렌더(프로젝트 repo push만으로 반영, 대시보드 재배포 불필요)
  - 외부 공유: 문서별 **공개 토글**(노션 게시처럼). 공개상태는 구글시트에 저장 → 즉시 적용. `/share/[slug]`만 middleware 제외
  - 댓글=Cusdis, 투표=Tally/구글폼. 옵션: 자동만료·비번·조회기록
- 현 대시보드 스택 확인: Next.js 16 + Tailwind, 공용비번 쿠키 인증([middleware.ts](middleware.ts)), 구글시트([lib/sheet.ts](lib/sheet.ts))+팝빌 백엔드. 배포는 현재 `.command` CLI → 표준대로 GitHub 자동배포로 전환 예정
- **다음 작업**: ①독서모임 등 대상 프로젝트 private repo + push(버전관리 시작) ②대시보드 "프로젝트" 메뉴 + MD 보기 + 공유 토글 ③Apps Script에 시트 쓰기 추가 ④댓글·투표·팀원 가이드. 첫 시연 대상=독서모임 호스트 제안서

## 2026-06-22

**서비스 탭 개편 — "업무툴" → "큐앤뱅 서비스", 반보 추가**

- 탭 라벨 `업무툴` → `큐앤뱅 서비스`로 변경
- `WORK_TOOLS`에 반보 2장 추가: 반보(메인 banbo-preview.vercel.app)·반보 어드민(/admin.html, 비번 필요)
- 안내 문구도 "큐앤뱅이 만든 서비스·업무 도구 모음"으로 수정
- Vercel CLI로 프로덕션 배포(dashboard.qnbang.com) 반영 완료
