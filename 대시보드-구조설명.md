# 큐앤뱅 대시보드 — 어떻게 굴러가나 (전체 구조)

> 비전공자용. 한 번 읽으면 "이게 왜 이렇게 도나"가 머리에 들어오게.

---

## 0. 한 장 요약

```
   너 / 라크          ┌─────────────────────┐
   (일 입력)  ───────▶│   과업 시트 (구글)    │  ← 회사의 단일 장부
                      │  과업·이력·매출·…탭    │     (모든 진실은 여기 한 곳)
                      └──────────┬──────────┘
                       읽기↑      ↓쓰기
            ┌──────────────┴───┐  └──────────┐
            │  시트 API(빠름)   │  │ Apps Script│
            │  = 화면 그릴 때    │  │ = 저장할 때 │
            └──────────────────┘  └────────────┘
                       │              ▲
                       ▼              │
            ┌─────────────────────────┴──┐
            │      대시보드 (웹 화면)       │
            │  보드 · 고객·영업 · 정산 …    │
            └────────────────────────────┘
```

**핵심 한 줄:** 모든 데이터는 **과업 시트** 한 곳에 있고, 대시보드는 **읽을 땐 빠른 길(시트 API)**, **쓸 땐 안전한 길(Apps Script)** 로 따로 다닌다.

---

## 1. 왜 시트가 중심인가 (단일 장부)

옛날엔 일이 GCP 한 곳, 구글 시트 한 곳에 따로 흩어져 갈라졌다(같은 일이 두 군데, 한쪽만 최신). 그래서 **시트 하나를 유일한 진실**로 정하고 전부 거기로 모았다. 대시보드도, 라크도, 너도 전부 이 시트만 본다 → 갈라질 일이 없다.

- **장점:** 진실이 한 곳 → 안 헷갈림. 시트라 손으로도 직접 고칠 수 있음.
- **단점:** 시트는 원래 "프로그램이 빠르게 읽는 용도"가 아니라, 빨리 읽으려면 아래 장치들이 필요.

---

## 2. 읽기 — 화면 그릴 때 (빠른 길)

**과정:** 대시보드가 화면을 그릴 때 → `시트 API`로 시트의 모든 탭을 **한 번에** 받아옴 → 2분간 책상에 펼쳐둠(캐시) → 화면에 뿌림.

- **누가 읽나:** 사람이 아니라 **서비스계정**(= 시트 읽으라고 만든 직원 봇 `lark-calendar-bot`). 시트에 "읽기" 권한만 줘둠.
- **얼마나 빠른가:** **0.4초.**
- 옛날엔 이걸 `Apps Script 웹앱`(아래 3번)으로 읽어서 **7~9초**였다. 그게 "대시보드 느림"의 진짜 범인. → 시트 API로 바꿔서 20배 빨라짐.

| | 시트 API (지금) | Apps Script 읽기 (옛날) |
|---|---|---|
| 속도 | **0.4초** | 7~9초 |
| 방식 | 구글 공식 직통 | 시트에 붙인 스크립트를 URL로 깨움 |
| 단점 | 서비스계정 권한 1번 셋업 | 호출마다 스크립트 부팅+셀 훑기(느림) |

---

## 3. 쓰기 — 저장할 때 (안전한 길)

**과정:** 보드에서 공위치 바꾸거나 단계 체크하면 → `Apps Script 웹앱`에 "이 줄, 이 칸만 이 값으로" POST → 시트 셀 수정 + 이력 한 줄 기록 → 캐시 비움(다음 읽기가 최신 받게).

- **왜 쓰기는 Apps Script로?** Apps Script가 "id로 줄 찾아 그 셀만 고치고, 드롭다운 유지하고, 이력 탭에 한 줄 더하는" 로직을 이미 들고 있어서 그대로 씀. 쓰기는 **가끔** 일어나니 조금 느려도 됨.
- **속도:** 셀 하나만 고치는 방식이라 **0.5초대.** (옛날엔 시트 전체를 다시 읽어 통째로 덮어써서 10~15초였는데, 그 낭비를 들어냄.)
- **장점:** 검증·이력 기록이 한 곳에 모여있음.
- **단점:** 읽기보다 느리고, 고치려면 따로 배포해야 함.

> 정리: **읽기 = 시트 API(빠름), 쓰기 = Apps Script(안전).** 둘이 길이 다른 게 핵심.

---

## 4. "항상 빠르게" 만드는 3중 장치

데이터를 매번 새로 부르면 그래도 0.4초씩 걸리니, 체감 0초로 만드는 장치 3개:

1. **캐시 + SWR** — 한 번 읽으면 2분간 그대로 즉시 보여줌. 2분 지나 낡으면? **일단 낡은 걸 보여주고 뒤에서 몰래 새로 받음**(너는 안 기다림). = "책상에 펼쳐둔 페이지 먼저 보여주고, 새 페이지는 뒤에서 가져옴."
2. **낙관적 UI** — 단계 체크/공위치 이동을 누르면, **저장 끝나길 안 기다리고 화면을 먼저 바꿔줌.** 저장은 뒤에서 조용히. (실패하면 되돌림.) = "도장 찍기 전에 '찍힌 셈 치고' 보여주기."
3. **localStorage 즉시그리기** — 지난번 본 보드를 브라우저에 저장 → 다음에 열면 **0초**에 그걸 먼저 그리고, 진짜 최신은 뒤에서 갱신.

이 셋 + 빠른 시트 API = **사실상 항상 즉시.**

---

## 5. 데이터 모델 (개념 정리)

화면에 보이는 카드 한 장이 시트의 한 줄이다. 그 구조:

- **프로젝트 = 과업** — 한 덩어리의 일(예: "망원 야간보물찾기"). 중간에 "과업명" 같은 층을 따로 안 둠(군더더기라 없앰).
- **할일 흐름** — 그 일의 단계들. `✓완료` / `▶지금` / `○예정`. 넘기면 지나온 건 ✓로 **쌓여서 이력**이 됨. → 카드 큰 글씨 = 지금 ▶단계(= 지금 뭘 할지).
- **현재상태** — 할일 단계가 비어있으면(예: 고객 답 기다리는 중) 그게 자동으로 "지금 단계"로 흡수됨. 손대는 순간 진짜 단계로 굳음.
- **공위치** — 공이 누구 코트에 있나: 받은일 · 시작전 · 내작업 · 내회신 · 고객대기 · 보류 · 완수. 보드의 칸이 이거.
- **분류** — 대행 / 자체 / 도구 / 리서치 / 자체사업. (필터용)

---

## 6. 화면(탭) 구조

- **🛰️ 사무실(보드)** — 공위치별 칸에 카드. 메인. 카드 클릭 → 여정 패널(할일 흐름·이력·정보수정).
- **👥 고객·영업** — 영업 중(리드·팔로업·계약완료) → 계약 진행중 → 완수. *(영업 탭을 여기로 합침.)*
- **💰 정산** — 매출·미수금·고정비·월별.
- **📁 프로젝트 / 🔗 허브 / 🔁 공유 / 🧰 도구** — 보조.

---

## 7. 한 줄 용어집

| 용어 | 쉬운 말 |
|---|---|
| 과업 시트 | 회사의 단일 장부(모든 일이 적힌 큰 노트) |
| 서비스계정 | 사람 대신 프로그램이 구글에 로그인하는 봇 직원 |
| 시트 API | 구글이 주는 "시트 직통 읽기/쓰기" 공식 통로(빠름) |
| Apps Script 웹앱 | 시트에 붙인 스크립트를 URL로 깨워 쓰는 것(느림, 쓰기 전용으로 남김) |
| 캐시 | 방금 읽은 걸 잠깐 책상에 펼쳐두기 |
| SWR | 낡은 거 일단 보여주고 뒤에서 새로 받기 |
| 낙관적 UI | 서버 답 안 기다리고 화면 먼저 바꾸기 |
| Vercel | 이 웹을 띄워주는 곳(배포 서버) |

---

## 8. 손볼 때 어디를 보나 (개발 메모)

- 읽기 방식: `lib/sheetCache.ts` (시트 API + 캐시)
- 보드 화면·여정 패널·낙관적 UI·localStorage: `app/components/OfficeBoardView.tsx`
- 쓰기 라우트: `app/api/office/update`(수정), `app/api/office/step`(단계·이력), `.../complete`(완수)
- 데이터 가공: `lib/office.ts`(과업·이력), `lib/money.ts`(돈), `lib/crm.ts`·`lib/sales.ts`(고객·영업)
- 환경변수(Vercel): `SHEET_ID`, `GOOGLE_SA_JSON`(서비스계정 키), 쓰기용 `SHEET_WRITE_URL`
- 배포: `대시보드 배포.command` 더블클릭 (커밋작성자 교정→빌드→업로드)
